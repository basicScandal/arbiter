---
phase: 08-e2e-pipeline-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_e2e_moe_scoring.py
  - tests/test_e2e_event_wiring.py
autonomous: true

must_haves:
  truths:
    - "A test validates MoE scoring with 3 mock providers returning different scores, and ScoreAggregator produces a weighted result through the full ScoringPipeline event path"
    - "A test verifies that all 14 sub-pipeline EventBus subscriptions wired in setup() are connected and responsive to their trigger events"
    - "Wiring regression tests catch a subscription that fails to register (missing event type in _subscribers dict)"
  artifacts:
    - path: "tests/test_e2e_moe_scoring.py"
      provides: "MoE integration test through pipeline event path (E2E-02)"
      contains: "test_moe_three_providers_through_pipeline"
    - path: "tests/test_e2e_event_wiring.py"
      provides: "Event wiring regression tests for all sub-pipeline subscriptions (E2E-03)"
      contains: "test_all_sub_pipeline_subscriptions_registered"
  key_links:
    - from: "tests/test_e2e_moe_scoring.py"
      to: "src/scoring/moe_engine.py"
      via: "MoEScoringEngine passed to ScoringPipeline, invoked on observation_verified"
      pattern: "MoEScoringEngine\\(providers="
    - from: "tests/test_e2e_moe_scoring.py"
      to: "src/scoring/aggregator.py"
      via: "ScoreAggregator.aggregate_criterion called for each criterion across providers"
      pattern: "aggregate_criterion"
    - from: "tests/test_e2e_event_wiring.py"
      to: "src/defense/pipeline.py"
      via: "DefensePipeline.setup() subscribes to 4 event types"
      pattern: "defense.*setup.*event_bus"
    - from: "tests/test_e2e_event_wiring.py"
      to: "src/commentary/pipeline.py"
      via: "CommentaryPipeline.setup() subscribes to 5 event types"
      pattern: "commentary.*setup.*event_bus"
    - from: "tests/test_e2e_event_wiring.py"
      to: "src/scoring/pipeline.py"
      via: "ScoringPipeline.setup() subscribes to 2 event types"
      pattern: "scoring.*setup.*event_bus"
    - from: "tests/test_e2e_event_wiring.py"
      to: "src/memory/pipeline.py"
      via: "DeliberationPipeline.setup() subscribes to 2 event types"
      pattern: "deliberation.*setup.*event_bus"
---

<objective>
Write E2E tests for MoE multi-provider scoring through the full ScoringPipeline event path, and event wiring regression tests verifying all 14 sub-pipeline EventBus subscriptions are connected and responsive.

Purpose: Validates the MoE path that is wired but never tested end-to-end through the event bus, and catches subscription wiring regressions (event name typos) that would silently break the pipeline.
Output: Two test files covering E2E-02 (MoE integration) and E2E-03 (event wiring regression).
</objective>

<execution_context>
@/Users/scandal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/scandal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-e2e-pipeline-coverage/08-RESEARCH.md

@tests/conftest.py
@tests/helpers/event_collector.py
@tests/test_scoring_aggregator.py
@tests/test_scoring_pipeline.py
@src/scoring/pipeline.py
@src/scoring/moe_engine.py
@src/scoring/aggregator.py
@src/scoring/engine.py
@src/scoring/models.py
@src/scoring/rubric.py
@src/defense/pipeline.py
@src/commentary/pipeline.py
@src/memory/pipeline.py
@src/capture/event_bus.py
@src/capture/models.py
@src/defense/models.py
@src/providers/base.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: MoE integration test through pipeline event path (E2E-02)</name>
  <files>tests/test_e2e_moe_scoring.py</files>
  <action>
Create `tests/test_e2e_moe_scoring.py` that tests the MoE scoring path THROUGH the ScoringPipeline event bus wiring (not just calling MoEScoringEngine.score() directly like existing unit tests do).

**Reusable helper (top of file):**

```python
def _make_provider(name: str, scores: dict[str, float]) -> MagicMock:
    """Create a mock LLMProvider returning JSON with specific criterion scores."""
    provider = MagicMock()
    type(provider).name = PropertyMock(return_value=name)
    criteria = [
        {"name": n, "score": s, "justification": f"Evidence for {n}"}
        for n, s in scores.items()
    ]
    provider.generate = AsyncMock(return_value=json.dumps({"criteria": criteria}))
    return provider
```

**Fixture: `sanitized`** — Same as test_scoring_aggregator.py (SanitizedOutput with team_name="TestTeam", observations=["Built a solid tool"], demo_duration=180.0).

**Fixture: `mock_display`** — Same pattern as test_scoring_pipeline.py (MagicMock(spec=DisplayServer) with AsyncMock methods).

**Test: `test_moe_three_providers_through_pipeline`**

The key difference from existing MoEScoringEngine unit tests: this test drives the MoE path through the event bus.

1. Create 3 mock providers with different scores:
   - `gemini`: Technical Execution=8.0, Innovation=7.0, Demo Quality=6.0
   - `claude`: Technical Execution=7.0, Innovation=8.0, Demo Quality=7.0
   - `openai`: Technical Execution=9.0, Innovation=6.0, Demo Quality=5.0
2. Create `MoEScoringEngine(providers=[gemini, claude, openai])`.
3. Create `ScoringPipeline(api_key="test", display=mock_display, moe_engine=moe)`.
4. Mock `scoring._store.save = AsyncMock()`.
5. `await scoring.setup(event_bus)`.
6. Publish `ObservationVerified(output=sanitized)` via `event_bus.publish()`.
7. `await event_collector.wait_for("scoring_complete", timeout=5.0)`.
8. Assert the ScoringComplete event has a scorecard where:
   - `scorecard.team_name == "TestTeam"`
   - `scorecard.total_score > 0`
   - `len(scorecard.criteria) == 3`
   - Each criterion score is between 0.0 and 10.0
9. Assert all three providers' `generate` was called exactly once.
10. Assert `scoring._store.save` was called with the aggregated scorecard.

**Test: `test_moe_aggregated_score_between_extremes`**

Same setup but verify that the aggregated per-criterion scores are between the min and max of the individual providers (after calibration, approximately):

1. Get the scorecard from the ScoringComplete event.
2. For "Technical Execution" (scores 8.0, 7.0, 9.0), aggregated should be between ~5.5 and ~9.0 (calibration shifts values).
3. For "Innovation" (scores 7.0, 8.0, 6.0), aggregated should be in a reasonable range.
4. Assert `scorecard.total_score` is a reasonable weighted sum (not 5.0 which would indicate fallback).

**Test: `test_moe_one_provider_fails_uses_remaining`**

1. Create 3 providers, one raises Exception on generate.
2. Wire through pipeline and publish ObservationVerified.
3. Wait for scoring_complete.
4. Assert scorecard is NOT the fallback (total_score != 5.0) — remaining providers produced valid scores.

**Test: `test_moe_all_providers_fail_produces_fallback`**

1. Create 3 providers, ALL raise Exception.
2. Wire through pipeline.
3. Publish ObservationVerified.
4. The scoring pipeline's `_on_observation_verified` has a try/except that returns early when scoring fails. Since the MoE engine's fallback scorecard still gets returned (it catches all errors internally), check that either:
   - scoring_complete event fires with a fallback scorecard (total_score == 5.0), OR
   - If the scoring_complete event does NOT fire (because the return statement prevents publish), verify that no scoring_complete event appears within a short timeout.

Use `@pytest.mark.timeout(15)` on all tests.
  </action>
  <verify>
Run `pytest tests/test_e2e_moe_scoring.py -v` and all tests pass. Verify no real API calls are made (all providers are mocked).
  </verify>
  <done>
A test file validates MoE scoring with 3 mock providers through the full ScoringPipeline event bus path, verifying aggregated scores, provider failure handling, and event publication.
  </done>
</task>

<task type="auto">
  <name>Task 2: Event wiring regression tests (E2E-03)</name>
  <files>tests/test_e2e_event_wiring.py</files>
  <action>
Create `tests/test_e2e_event_wiring.py` that verifies all 14 sub-pipeline EventBus subscriptions are connected and responsive.

**Scope decision (per research recommendation):** Test the 4 sub-pipeline `setup()` wiring (14 subscriptions across defense/commentary/scoring/deliberation). The 8 CapturePipeline-direct subscriptions are simple forwarding handlers covered by existing unit tests. Document this scoping decision in the file docstring.

**Complete subscription inventory (14 subscriptions to verify):**

DefensePipeline (4):
- `key_frame_detected` -> `_on_key_frame`
- `transcript_received` -> `_on_transcript`
- `demo_started` -> `_on_demo_started`
- `demo_stopped` -> `_on_demo_stopped`

CommentaryPipeline (5):
- `observation_verified` -> `_on_observation_verified`
- `qa_requested` -> `_on_qa_requested`
- `injection_detected` -> `_on_injection_detected`
- `demo_started` -> `_on_demo_started`
- `demo_stopped` -> `_on_demo_stopped`

ScoringPipeline (2):
- `observation_verified` -> `_on_observation_verified`
- `commentary_delivered` -> `_on_commentary_delivered`

DeliberationPipeline (3):
- `observation_verified` -> `_on_observation_verified`
- `deliberation_requested` -> `_on_deliberation_requested`

**Wait — the research says 2 for deliberation but there are indeed only 2 subscriptions in the code. Let me verify:** Yes, deliberation subscribes to `observation_verified` and `deliberation_requested`. That's 4+5+2+2 = 13 (not 14). The research says 14 across the sub-pipelines; recount: defense=4, commentary=5, scoring=2, deliberation=2 = 13. Use 13 as the correct count. (The research's "14" may have included the subscribe_all for EventCollector.)

**Test: `test_all_sub_pipeline_subscriptions_registered`**

The "registration" test -- verifies every expected event type has at least one handler registered after all setup() calls:

1. Create fresh EventBus.
2. Create all 4 sub-pipelines with mocked dependencies:
   - DefensePipeline(api_key="test") -- gemini_session=None is fine for this test
   - CommentaryPipeline with mocked _tts and _display (patch connect/start as AsyncMock before setup)
   - ScoringPipeline with mock_display
   - DeliberationPipeline with mock_display
3. Call `await pipeline.setup(event_bus)` for each.
4. Define the expected subscription set:
   ```python
   expected_event_types = {
       "key_frame_detected", "transcript_received",
       "demo_started", "demo_stopped",
       "observation_verified", "qa_requested", "injection_detected",
       "commentary_delivered", "deliberation_requested",
   }
   ```
5. Assert `expected_event_types.issubset(set(event_bus._subscribers.keys()))`.
6. For each event type, assert `len(event_bus._subscribers[event_type]) > 0`.

**Test: `test_defense_subscriptions_responsive`**

Verify defense pipeline handlers actually respond (not just registered but callable):

1. Create DefensePipeline, wire to EventBus.
2. Replace each handler with AsyncMock (`defense._on_demo_started = AsyncMock()`, etc.)
3. For each event type, publish a minimal event of that type and `await asyncio.sleep(0)`.
4. Assert each handler AsyncMock was called exactly once.
5. Event instances to use:
   - `DemoStarted(team_name="Test")`
   - `DemoStopped(team_name="Test", duration=1.0)`
   - `KeyFrameDetected(frame=FrameData(jpeg_data=b"fake", width=1, height=1, timestamp=0.0))`
   - `TranscriptReceived(segment=TranscriptSegment(text="hello", timestamp=0.0))`

**Test: `test_commentary_subscriptions_responsive`**

Same pattern for CommentaryPipeline's 5 subscriptions:
- Publish `ObservationVerified(output=sanitized)`, `QARequested(team_name="T")`, `InjectionDetected(attempt=...)`, `DemoStarted(team_name="T")`, `DemoStopped(team_name="T", duration=1.0)`.
- After setup and handler replacement with AsyncMocks, publish each and assert called.

**Test: `test_scoring_subscriptions_responsive`**

ScoringPipeline's 2 subscriptions:
- Publish `ObservationVerified(output=sanitized)` and `CommentaryDelivered(team_name="T", commentary_text="text")`.
- Assert both handlers called.

**Test: `test_deliberation_subscriptions_responsive`**

DeliberationPipeline's 2 subscriptions:
- Publish `ObservationVerified(output=sanitized)` and `DeliberationRequested()`.
- Assert both handlers called.

**Test: `test_subscriber_count_matches_expected`**

A count-based regression guard:

1. Wire all 4 sub-pipelines to the same EventBus.
2. Count total subscribers: `sum(len(v) for v in event_bus._subscribers.values())`.
3. Assert total == 13 (the exact count). If a future change adds or removes a subscription, this test will catch it and require updating the count.
4. Comment explaining why this exact count matters.

**Important:** For InjectionDetected events needed in test_commentary_subscriptions_responsive, create a minimal InjectionAttempt fixture: `InjectionAttempt(timestamp=0.0, injection_type="visual", content="test", pattern="test", confidence="high", team_name="Test")`.

Use `@pytest.mark.timeout(10)` on all tests (these are lightweight).
  </action>
  <verify>
Run `pytest tests/test_e2e_event_wiring.py -v` and all tests pass. Verify the subscription count matches expectations.
  </verify>
  <done>
A test file verifies all 13 sub-pipeline EventBus subscriptions are registered and responsive to their trigger events, with a count-based regression guard that catches additions or removals.
  </done>
</task>

</tasks>

<verification>
```bash
# Run both E2E test files from this plan
pytest tests/test_e2e_moe_scoring.py tests/test_e2e_event_wiring.py -v

# Run ALL E2E test files from Phase 8
pytest tests/test_e2e_pipeline_chain.py tests/test_e2e_task_draining.py tests/test_e2e_moe_scoring.py tests/test_e2e_event_wiring.py -v

# Full test suite regression check
pytest tests/ -v --timeout=30

# Verify parallel compatibility
pytest tests/ -n auto --timeout=30
```
</verification>

<success_criteria>
1. `test_moe_three_providers_through_pipeline` validates 3 mock providers through the ScoringPipeline event bus path with aggregated weighted result
2. `test_all_sub_pipeline_subscriptions_registered` verifies all expected event types have handlers after setup()
3. Responsive tests verify each subscription handler is actually called when its trigger event fires
4. Subscriber count regression guard catches additions/removals of subscriptions
5. All tests pass in parallel execution (pytest -n auto)
</success_criteria>

<output>
After completion, create `.planning/phases/08-e2e-pipeline-coverage/08-02-SUMMARY.md`
</output>
