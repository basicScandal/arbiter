---
phase: 06-venue-hardening
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/capture/models.py
  - src/capture/demo_machine.py
  - src/operator/cli.py
  - src/operator/tui.py
  - src/capture/pipeline.py
  - src/commentary/pipeline.py
autonomous: true

must_haves:
  truths:
    - "Operator can pause a running demo and resume it without losing session state"
    - "Operator can stop a paused demo (going directly from paused to stopped)"
    - "TUI shows pause/resume keybindings (Ctrl+P, Ctrl+O) and updates header state to PAUSED"
    - "CLI accepts pause and resume commands with state-aware hints"
    - "Commentary pipeline degrades to text-only mode when TTS is unavailable (ServiceHealth integration)"
    - "Camera and audio capture are muted/paused during pause state"
  artifacts:
    - path: "src/capture/demo_machine.py"
      provides: "DemoMachine with paused state and pause_demo/resume_demo transitions"
      contains: "paused = State()"
    - path: "src/capture/models.py"
      provides: "DemoPaused and DemoResumed event types"
      contains: "class DemoPaused"
    - path: "src/operator/cli.py"
      provides: "OperatorCLI with pause and resume commands"
      contains: "_handle_pause"
    - path: "src/operator/tui.py"
      provides: "ArbiterTUI with pause/resume keybindings and PAUSED state display"
      contains: "action_send_pause"
    - path: "src/capture/pipeline.py"
      provides: "CapturePipeline with pause/resume event handlers that mute audio and pause camera"
      contains: "_on_demo_paused"
    - path: "src/commentary/pipeline.py"
      provides: "CommentaryPipeline with ServiceHealth-aware text-only degradation"
      contains: "ServiceHealth"
  key_links:
    - from: "src/capture/demo_machine.py"
      to: "src/capture/pipeline.py"
      via: "DemoPaused/DemoResumed events subscribed in pipeline.run()"
      pattern: "demo_paused"
    - from: "src/operator/tui.py"
      to: "src/capture/demo_machine.py"
      via: "pause_demo/resume_demo state machine transitions"
      pattern: "pause_demo\\|resume_demo"
    - from: "src/commentary/pipeline.py"
      to: "src/resilience/health.py"
      via: "ServiceHealth checks before TTS speak"
      pattern: "is_healthy"
---

<objective>
Add operator pause/resume controls with full state machine integration, wire capture components to react to pause/resume events, and add degraded-mode text-only path when TTS is unhealthy.

Purpose: The operator needs to temporarily halt Arbiter during venue technical difficulties without losing the demo session. When TTS is confirmed down, the commentary pipeline should skip TTS and deliver text-only to the display server, keeping the audience informed.

Output: Extended DemoMachine with paused state. Pause/resume commands in CLI and TUI. Capture pipeline pause/resume handlers. Commentary pipeline degraded-mode path.
</objective>

<execution_context>
@/Users/scandal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/scandal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-venue-hardening/06-RESEARCH.md
@.planning/phases/06-venue-hardening/06-01-SUMMARY.md
@.planning/phases/06-venue-hardening/06-02-SUMMARY.md

@src/capture/demo_machine.py
@src/capture/models.py
@src/operator/cli.py
@src/operator/tui.py
@src/capture/pipeline.py
@src/commentary/pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add paused state to DemoMachine with event types and operator controls</name>
  <files>
    src/capture/models.py
    src/capture/demo_machine.py
    src/operator/cli.py
    src/operator/tui.py
  </files>
  <action>
    1. Add DemoPaused and DemoResumed event types to src/capture/models.py:
       - class DemoPaused(CaptureEvent): event_type = "demo_paused", team_name: str
       - class DemoResumed(CaptureEvent): event_type = "demo_resumed", team_name: str

    2. Extend DemoMachine in src/capture/demo_machine.py:
       - Add paused = State() between capturing and stopped
       - Add transitions:
         - pause_demo = capturing.to(paused)
         - resume_demo = paused.to(capturing)
         - Modify stop_demo to allow: capturing.to(stopped) | paused.to(stopped)
       - Add on_enter_paused callback that publishes DemoPaused(team_name=current_session.team_name)
       - Add on_exit_paused (when resuming) that publishes DemoResumed(team_name=current_session.team_name)
       - Import DemoPaused, DemoResumed from models
       - NOTE: Use the python-statemachine | combinator for stop_demo:
         stop_demo = capturing.to(stopped) | paused.to(stopped)

    3. Add pause/resume commands to src/operator/cli.py:
       - Add _handle_pause() method:
         - Calls self.demo_machine.send("pause_demo")
         - Prints "Demo paused for team: {team_name}"
       - Add _handle_resume() method:
         - Calls self.demo_machine.send("resume_demo")
         - Prints "Demo resumed for team: {team_name}"
       - Add "pause" and "resume" to the command dispatch in run()
       - Add pause/resume to _print_help() output
       - Add state hints for pause/resume:
         - ("pause", "idle"): "No demo in progress."
         - ("pause", "stopped"): "Demo already stopped."
         - ("pause", "paused"): "Demo already paused."
         - ("resume", "idle"): "No demo to resume."
         - ("resume", "capturing"): "Demo is already running."
         - ("resume", "stopped"): "Demo is stopped. Use 'reset' to prepare next demo."

    4. Add pause/resume to src/operator/tui.py:
       - Add keybindings:
         - Binding("ctrl+p", "send_pause", "Pause", show=True)
         - Binding("ctrl+o", "send_resume", "Resume", show=True)
       - Add action_send_pause and action_send_resume methods (same pattern as action_send_stop)
       - Add "pause" and "resume" to the on_command_submitted dispatch
       - Add _handle_pause() and _handle_resume() methods:
         - _handle_pause: send "pause_demo" to demo_machine, log
         - _handle_resume: send "resume_demo" to demo_machine, log
       - Add demo_paused and demo_resumed event handling in on_bus_event:
         - demo_paused: set header.state = "PAUSED", sidebar.state = "PAUSED"
         - demo_resumed: set header.state = "CAPTURING", sidebar.state = "CAPTURING"
       - Add pause/resume to _print_help()
       - Add state hints for pause/resume to _get_state_hint()
  </action>
  <verify>
    Run: python -c "from src.capture.demo_machine import DemoMachine; m = DemoMachine(); m.start_demo(team_name='Test'); m.pause_demo(); print(m.current_state.id); m.resume_demo(); print(m.current_state.id); m.stop_demo(); print(m.current_state.id); print('OK')" prints paused, capturing, stopped, OK.
    Run: python -c "from src.capture.demo_machine import DemoMachine; m = DemoMachine(); m.start_demo(team_name='Test'); m.pause_demo(); m.stop_demo(); print(m.current_state.id); print('OK')" prints stopped, OK (stop from paused works).
    Run: python -c "from src.capture.models import DemoPaused, DemoResumed; print('OK')" succeeds.
    Run: python -c "from src.operator.cli import OperatorCLI; from src.operator.tui import ArbiterTUI; print('OK')" succeeds.
  </verify>
  <done>DemoMachine has paused state with pause_demo/resume_demo transitions. DemoPaused and DemoResumed event types exist. Stop works from both capturing and paused states. CLI has pause/resume commands. TUI has Ctrl+P/Ctrl+O keybindings and PAUSED header state display.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pause/resume into capture pipeline and add degraded-mode text-only commentary</name>
  <files>
    src/capture/pipeline.py
    src/commentary/pipeline.py
  </files>
  <action>
    1. Add pause/resume event handlers to src/capture/pipeline.py:
       - Import DemoPaused, DemoResumed from models
       - Add _on_demo_paused handler:
         - Mute audio capture: self.audio.mute()
         - Set camera paused flag: self.camera.pause() (or set a _paused flag if camera does not have pause -- check camera.py for existing API. If no pause method, just mute audio and log. Camera resources stay open per research guidance -- do NOT close cv2.VideoCapture.)
         - Log: "Capture paused for team: {team_name}"
       - Add _on_demo_resumed handler:
         - Unmute audio: self.audio.unmute()
         - Resume camera: self.camera.resume() (or unset _paused flag)
         - Log: "Capture resumed for team: {team_name}"
       - Subscribe in run(): event_bus.subscribe("demo_paused", self._on_demo_paused) and event_bus.subscribe("demo_resumed", self._on_demo_resumed)

    2. Add degraded-mode text-only path to src/commentary/pipeline.py:
       - Import ServiceHealth and default_health from src.resilience.health
       - In setup(), after TTS connect try/except:
         - If TTS connection succeeded, call default_health.mark_healthy("cartesia_tts")
         - If TTS connection failed, call default_health.mark_unhealthy("cartesia_tts")
       - In _on_observation_verified(), modify the sentence delivery loop:
         - Check default_health.is_healthy("cartesia_tts") before each TTS speak call
         - If healthy: use existing asyncio.gather(tts.speak, display.push_commentary) pattern
         - If unhealthy: skip TTS speak, only call display.push_commentary (text-only mode)
         - Log at INFO level when entering text-only mode: "TTS unhealthy -- delivering text-only commentary"
       - After a successful TTS speak, call default_health.mark_healthy("cartesia_tts")
       - After a failed TTS speak (TTSEngine.speak() catches its own errors), check if the fallback also failed. Since TTSEngine logs but does not re-raise, the health tracking should be inside TTSEngine or the pipeline should check a signal. Simplest approach: wrap the tts.speak() call in a try/except in the pipeline, mark_unhealthy on exception. But TTSEngine already catches -- so instead, add a flag: after each speak, check if self._tts._connected is still True. If False, mark_unhealthy.
       - In _on_qa_requested(), apply the same text-only degradation for Q&A question delivery

    Important: The text-only path still pushes to the display server -- the audience sees the text even if they cannot hear TTS. This satisfies REL-02 (degraded mode behavior).
  </action>
  <verify>
    Run: python -c "from src.capture.pipeline import CapturePipeline; print('OK')" succeeds.
    Run: python -c "from src.commentary.pipeline import CommentaryPipeline; print('OK')" succeeds.
    Verify pause/resume subscriptions: grep 'demo_paused\|demo_resumed' src/capture/pipeline.py shows both subscriptions.
    Verify health integration: grep 'ServiceHealth\|is_healthy\|mark_healthy\|mark_unhealthy' src/commentary/pipeline.py shows health tracking.
  </verify>
  <done>Capture pipeline mutes audio and pauses camera on demo_paused, unmutes and resumes on demo_resumed. Commentary pipeline checks ServiceHealth before TTS and degrades to text-only when TTS is unhealthy. Display server always receives text regardless of TTS health. Q&A delivery also degrades gracefully.</done>
</task>

</tasks>

<verification>
1. DemoMachine state flow: idle -> capturing -> paused -> capturing -> stopped (full pause/resume cycle)
2. DemoMachine shortcut: idle -> capturing -> paused -> stopped (stop from paused)
3. CLI commands: pause and resume accessible with correct state hints
4. TUI keybindings: Ctrl+P and Ctrl+O functional, PAUSED state displayed
5. Capture pipeline: audio muted on pause, unmuted on resume
6. Commentary pipeline: text-only delivery when ServiceHealth reports TTS unhealthy
</verification>

<success_criteria>
- DemoMachine has idle -> capturing -> paused -> stopped states with all valid transitions
- DemoPaused and DemoResumed events published on state transitions
- Operator can pause/resume via CLI commands and TUI keybindings
- Camera and audio capture are suspended during pause (resources stay open)
- Commentary degrades to text-only when TTS is unhealthy
- Display server always receives text regardless of TTS state
</success_criteria>

<output>
After completion, create .planning/phases/06-venue-hardening/06-03-SUMMARY.md
</output>
